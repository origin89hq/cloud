{
  "name": "origin89-cloud",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8791",
    "check": "wrangler types && tsc --noEmit",
    "test": "vitest run && node --test scripts/*.test.mjs",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "deploy:config": "node scripts/deployment-config.mjs",
    "deploy:remote": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "@origin89/cloud": "workspace:*",
    "jose": "6.2.12",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "jsonc-parser": "3.3.1",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.140.0"
  },
  "license": "UNLICENSED"
}
