name: Checks
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
jobs:
  node:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: ./.github/actions/setup-node
      - name: Validate pending changesets
        if: hashFiles('.changeset/*.md', '!.changeset/README.md') != ''
        env:
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          git show-ref --verify --quiet "refs/heads/$BASE_BRANCH" || git branch --no-track "$BASE_BRANCH" "origin/$BASE_BRANCH"
          pnpm changeset status
      - run: pnpm check
      - run: pnpm run package

  deploy:
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    needs: node
    runs-on: ubuntu-latest
    timeout-minutes: 20
    concurrency:
      group: cloud-production
      cancel-in-progress: false
    env:
      WRANGLER_SEND_METRICS: 'false'
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      CLOUD_WORKER_NAME: ${{ vars.CLOUD_WORKER_NAME }}
      CLOUD_HOSTNAME: ${{ vars.CLOUD_HOSTNAME }}
      CLOUD_DATABASE_NAME: ${{ vars.CLOUD_DATABASE_NAME }}
      CLOUD_DATABASE_ID: ${{ vars.CLOUD_DATABASE_ID }}
      WORKOS_CLIENT_ID: ${{ vars.WORKOS_CLIENT_ID }}
      WORKOS_ISSUER: ${{ vars.WORKOS_ISSUER }}
    steps:
      - name: Require the current main commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          current=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq .object.sha)
          test "$current" = "$GITHUB_SHA" || { echo 'Deploy the current main commit; this run is stale.'; exit 1; }
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-node
      - run: pnpm --filter origin89-cloud deploy:config production
      - name: Migrate and deploy the cloud Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          WORKOS_API_KEY: ${{ secrets.WORKOS_API_KEY }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo 'Configure the CLOUDFLARE_API_TOKEN repository secret.'; exit 1; }
          test -n "$WORKOS_API_KEY" || { echo 'Configure the WORKOS_API_KEY repository secret.'; exit 1; }
          pnpm --filter origin89-cloud deploy:remote production
